import React from 'react';
import { ServerBuilderStudio } from '../components/server-builder/ServerBuilderStudio';

export const ServerBuilderPage: React.FC = () => {
  return <ServerBuilderStudio />;
};

export default ServerBuilderPage;
