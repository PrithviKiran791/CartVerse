/**
 * FontSelector — disabled.
 * CartVerse uses Inter Tight as the fixed global font system.
 * This component is retained as a null stub to avoid import errors.
 */
import React from 'react';

export const FontSelector: React.FC = () => null;

export default FontSelector;
